# 1. CLI 交互指南

> **📚 学习目标**
> 阅读本文后，你将能够：
> - 熟练使用所有 CLI 斜杠命令
> - 掌握高级交互技巧提升使用效率
> - 学会管理后台任务
> - 利用快捷键提升操作速度

## 交互界面说明

Hermes CLI 界面由以下部分组成：
1. **欢迎横幅**：启动时显示，包含版本、模型、工具、技能等信息
2. **输入提示符**：用户输入命令和问题的地方
3. **思考 spinner**：模型处理时显示的动画，包含当前动作和可爱的表情
4. **工具输出**：工具调用的结果，以特殊前缀标识
5. **响应框**：模型的最终响应，以边框包围显示

## 斜杠命令系统

所有斜杠命令以 `/` 开头，支持自动补全，按 `Tab` 键可以查看可用命令和参数。

**为什么需要斜杠命令？**
斜杠命令提供了一种明确的控制CLI的方式，和自然语言对话区分开，避免模型误解析你的控制指令，同时操作更高效。

### 会话类命令

| 命令 | 别名 | 说明 | 示例 |
|------|------|------|------|
| `/clear` | `/c` | 清空当前会话上下文 | `/clear` |
| `/history` | `/h` | 显示当前会话历史 | `/history` |
| `/save` |  | 保存当前会话到文件 | `/save my_session.json` |
| `/load` |  | 从文件加载会话 | `/load my_session.json` |
| `/exit` | `/quit`、`/q` | 退出 CLI | `/exit` |

### 配置类命令

| 命令 | 别名 | 说明 | 示例 |
|------|------|------|------|
| `/set` |  | 设置配置项 | `/set display.skin ares` |
| `/get` |  | 查看配置项 | `/get model.default` |
| `/model` | `/m` | 切换默认模型 | `/model openai/gpt-4o` |
| `/skin` |  | 切换主题皮肤 | `/skin mono` |
| `/setup` |  | 重新运行配置向导 | `/setup` |

### 工具与技能类命令

| 命令 | 别名 | 说明 | 示例 |
|------|------|------|------|
| `/tools` |  | 管理工具集 | `/tools list` |
| `/skills` |  | 管理技能 | `/skills install python-refactor` |
| `/enable` |  | 启用工具/技能 | `/enable tool browser` |
| `/disable` |  | 禁用工具/技能 | `/disable skill alpha-loop` |

### 信息类命令

| 命令 | 别名 | 说明 | 示例 |
|------|------|------|------|
| `/help` | `/?` | 显示帮助信息 | `/help model` |
| `/version` | `/v` | 显示版本信息 | `/version` |
| `/status` |  | 显示当前状态 | `/status` |
| `/models` |  | 列出可用模型 | `/models` |
| `/skins` |  | 列出可用皮肤 | `/skins` |

## 高级交互功能

### 多行输入
输入 `"""` 然后回车，进入多行输入模式，输入结束后再输入 `"""` 并回车发送。适合输入长文本或代码块。

### 命令补全
按 `Tab` 键自动补全命令、参数、模型名、皮肤名等，支持模糊匹配。

### 历史记录
按 `↑` 和 `↓` 键浏览历史输入，按 `Ctrl+R` 搜索历史输入。

### 输出控制
- 按 `Ctrl+C` 中断当前模型请求或工具执行
- 长输出会自动分页，按 `空格键` 翻页，按 `q` 退出分页

## 背景任务管理

**为什么需要后台任务？**
当你需要运行耗时较长的任务（比如训练模型、下载大文件）时，后台任务可以让你在任务运行的同时继续使用CLI进行其他操作，不需要等待任务完成。

### 运行后台任务
在命令末尾添加 `&` 或者使用 `background=true` 参数，让任务在后台运行：
```
> 运行 python app.py 后台执行 &
```
或者
```
> terminal(command="python app.py", background=true)
```

### 查看后台任务
```
/jobs
```
列出所有正在运行的后台任务。

### 查看任务输出
```
/jobs output 1
```
查看 ID 为 1 的后台任务的输出。

### 终止后台任务
```
/jobs kill 1
```
终止 ID 为 1 的后台任务。

### 等待任务完成
```
/jobs wait 1
```
等待 ID 为 1 的后台任务完成。

## 快捷键

| 快捷键 | 说明 |
|--------|------|
| `Ctrl+D` | 退出 CLI |
| `Ctrl+C` | 中断当前操作 |
| `Ctrl+R` | 搜索历史输入 |
| `Ctrl+L` | 清屏 |
| `Tab` | 自动补全 |
| `↑/↓` | 浏览历史输入 |
| `PageUp/PageDown` | 滚动输出历史 |

---

## ✍️ 练习
1. 练习使用 `/clear、/history、/save 三个会话命令，保存当前会话到文件
2. 尝试使用多行输入模式，输入一段包含多行代码的问题
3. 运行一个后台任务（比如 `sleep 30`），练习查看任务输出、终止任务等操作
4. 记住3个常用快捷键，提升操作效率

---

## ❓ 常见问题
### Q：如何复制CLI输出的内容？
A：直接用鼠标选中内容即可自动复制，或者使用 `Ctrl+Shift+C` 复制选中内容，`Ctrl+Shift+V` 粘贴内容。

### Q：历史输入记录存在哪里？
A：历史输入保存在 `~/.hermes/history` 文件中，可以直接编辑或删除不需要的历史记录。

### Q：后台任务在退出CLI后还会继续运行吗？
A：不会，退出CLI会自动终止所有正在运行的后台任务，如果需要任务持续运行，请使用系统的nohup或screen命令。

---

## 📖 导航
- 上一篇：[基础配置](../../intro/4-configuration.md)
- 下一篇：[网关使用指南](./2-gateway-guide.md)
- [返回目录](../SUMMARY.md)
